# 📚 คู่มือโครงสร้างโปรเจ็คและเอกสารอ้างอิง — KruTeekidCode Blog

ยินดีต้อนรับสู่คู่มือการทำงานของโปรเจ็คเว็บบล็อกและระบบคอร์สเรียน **KruTeekidCode** เอกสารฉบับนี้จัดทำขึ้นเพื่อแสดงโครงสร้างไดเรกทอรีอย่างละเอียด และวิธีการเพิ่มเนื้อหา/แก้ไขสไตล์ เพื่อให้คุณใช้เป็นข้อมูลอ้างอิงสำหรับแก้ไขเว็บไซต์ด้วยตนเองในอนาคตครับ

---

## 🌳 แผนผังโครงสร้างเว็บไซต์ (Directory Tree Diagram)

ด้านล่างนี้คือแผนผังโครงสร้างไฟล์และโฟลเดอร์ของโปรเจ็ค Next.js (App Router) นี้ครับ:

```text
kruteekidcode-blog/
├── app/                              # 🌐 โครงสร้างหน้าเว็บและเส้นทาง (App Router)
│   ├── about/                        # หน้าเกี่ยวกับฉัน (/about)
│   │   └── page.tsx
│   ├── blog/                         # หน้าระบบบล็อก (/blog)
│   │   ├── page.tsx                  # หน้ารายการบทความทั้งหมด
│   │   └── [slug]/                   # หน้าอ่านบทความตัวเต็ม (Dynamic Route)
│   │       └── page.tsx
│   ├── courses/                      # หน้าระบบหลักสูตร (/courses)
│   │   ├── page.tsx                  # หน้ารายการหลักสูตรทั้งหมด
│   │   └── [slug]/                   # หน้าเจาะลึกเนื้อหาบทเรียน (Dynamic Route)
│   │       └── page.tsx
│   ├── favicon.ico                   # ไอคอนแท็บเบราว์เซอร์
│   ├── globals.css                   # 🎨 ดีไซน์ซิสเต็มและสไตล์หลักของเว็บ (CSS)
│   ├── layout.tsx                    # โครงหลักของเว็บ (Navbar + Footer Wrapper)
│   ├── page.module.css               # สไตล์เฉพาะหน้าแรก (ไม่ได้ใช้งานหลัก)
│   ├── page.tsx                      # 🏠 หน้าแรกของเว็บ (Dynamic Homepage)
│   ├── robots.ts                     # 🤖 ควบคุม Search Engine Bots (SEO)
│   └── sitemap.ts                    # 🗺️ แผนผังเว็บไซต์อัตโนมัติ (SEO)
│
├── components/                       # 🧩 ชิ้นส่วนเว็บที่นำกลับมาใช้ใหม่ได้ (UI Components)
│   ├── BlogCard.tsx                  # การ์ดแสดงบทความย่อบนหน้าแรกและหน้าบล็อก
│   ├── CourseCard.tsx                # การ์ดแสดงวิชาเรียนย่อบนหน้าแรกและหน้าหลักสูตร
│   ├── Footer.tsx                    # แถบท้ายหน้าแสดงโลโก้ โซเชียล และลิขสิทธิ์
│   └── Navbar.tsx                    # แถบเมนูด้านบน (รองรับ Responsive มือถือ)
│
├── content/                          # 📝 แหล่งเก็บไฟล์เนื้อหาจริง (Markdown/MDX)
│   ├── about.mdx                     # ข้อมูลประวัติในหน้าเกี่ยวกับฉัน
│   ├── blog/                         # โฟลเดอร์เก็บไฟล์เนื้อหาบทความ (.mdx)
│   │   ├── ai-in-education.mdx
│   │   ├── getting-started-with-python.mdx
│   │   └── nextjs-first-app.mdx
│   └── courses/                      # โฟลเดอร์เก็บไฟล์เนื้อหาหลักสูตร (.mdx)
│       ├── html-css.mdx
│       └── python-beginner.mdx
│
├── lib/                              # ⚙️ โฟลเดอร์ฟังก์ชันเครื่องมือทำงานหลังบ้าน
│   └── content.ts                    # เครื่องมือดึงเนื้อหา MDX, หาคำอ่าน, จัดการวันภาษาไทย
│
├── public/                           # 📂 เก็บไฟล์ภาพและสื่อต่างๆ ของระบบ
│   ├── llms.txt                      # 🤖 ตัวดัชนีเนื้อหาเว็บสำหรับระบบค้นหา AI (AI Search Engine)
│   └── [รูปภาพอื่นๆ ที่ต้องการใส่ในบล็อก]
│
├── package.json                      # 📦 กำหนดเวอร์ชัน Library และคำสั่งรันระบบ
└── tsconfig.json                     # 🛠️ การตั้งค่าระบบภาษา TypeScript
```

---

## 📂 หน้าที่ของแต่ละ Directory และไฟล์หลัก

### 1. โฟลเดอร์ `app/` (Web Router & Layouts)
เป็นหัวใจหลักในการกำหนดโครงสร้างหน้าเพจของ Next.js 16 (App Router)
* **`app/layout.tsx`**: โครงร่างครอบเว็บไซต์ทั้งหมด ใส่ฟอนต์ **Inter** และเรียกใช้แถบเมนูหัวเว็บ (`Navbar`) และท้ายเว็บ (`Footer`) เพื่อให้แสดงผลเหมือนกันทุกหน้า
* **`app/globals.css`**: ดีไซน์ซิสเต็มของเว็บทั้งหมด โดยควบคุมสีผ่านตัวแปร CSS Variables ทั้งสไตล์หัวข้อ (`h1`-`h6`), ย่อหน้า (`p`), ลิงก์, ตาราง, และโค้ดโค้ดดิ้ง รวมถึงการควบคุม Responsive บนหน้าจอมือถือและแท็บเล็ต
* **`app/page.tsx`**: โค้ดหน้าแรกสุดของเว็บไซต์ ซึ่งจะดึงข้อมูลบทความล่าสุด 3 ชิ้น และวิชาเรียนล่าสุด 2 วิชามาจัดแสดงผลแบบอัตโนมัติ

### 2. โฟลเดอร์ `components/` (Reusable UI Components)
เก็บชิ้นส่วนขนาดเล็กของหน้าจอเพื่อให้โค้ดเป็นระเบียบและไม่ซ้ำซ้อน
* **`Navbar.tsx` & `Footer.tsx`**: แถบเมนูด้านบนและท้ายเว็บ โดย `Navbar` มีการเขียน JavaScript ในตัวเพื่อรองรับปุ่มแฮมเบอร์เกอร์สามขีดในหน้าจอมือถือ
* **`BlogCard.tsx` & `CourseCard.tsx`**: ออกแบบสไตล์ของการ์ดสรุปข้อมูลให้มีความสวยงาม มีปุ่มกดอ่าน และแสดงหมวดหมู่ (Tag)

### 3. โฟลเดอร์ `content/` (Content Database)
เปรียบเสมือนฐานข้อมูลของเว็บไซต์ โดยเก็บข้อมูลเป็นรูปแบบเอกสาร **MDX (Markdown + React Component)** ทำให้เขียนบทความได้ง่าย รวดเร็ว และไม่มีสไตล์โค้ดดิ้งมารบกวน
* ทุกครั้งที่คุณอยากเพิ่มบทความใหม่ ให้สร้างไฟล์ `.mdx` ลงในโฟลเดอร์ `content/blog/` ได้เลย
* ทุกครั้งที่คุณอยากเพิ่มหรือแก้ไขเนื้อหาหลักสูตร ให้สร้างหรือแก้ไขไฟล์ใน `content/courses/` ได้เลย

### 4. โฟลเดอร์ `lib/` (Backend Utilities)
* **`lib/content.ts`**: รวบรวมฟังก์ชันการทำงานส่วนลึก เช่น การสแกนไดเรกทอรีอ่านไฟล์บล็อก คัดแยกหัวข้อบล็อกตามวันที่ล่าสุด รวมถึงการใช้สูตรคำนวณระยะเวลาในการอ่านบทความ (เช่น `5 min`) และแปลงฟอร์แมตวันคริสต์ศักราชเป็นวันภาษาไทยปีพุทธศักราช (เช่น `2026-05-28` แปลงเป็น `28 พ.ค. 2569`)

---

## 🎨 ข้อมูลชุดสีประจำเว็บไซต์ (Theme Palette)

> [!IMPORTANT]
> **ไฟล์และตำแหน่งสำหรับแก้ไขชุดสี:** 
> คุณสามารถเข้าไปแก้ไขโทนสีทั้งหมดของเว็บไซต์ได้ในไฟล์ **[app/globals.css](file:///C:/โปรเจ็คเล่นสนุกของตี๋/Web Blog/kruteekidcode-blog/app/globals.css)** 
> โดยเปิดไฟล์แล้วแก้ไขรหัสสี Hex ในบล็อก **`:root`** (อยู่บริเวณช่วงเริ่มต้นไฟล์ บรรทัดที่ 9 ถึง 26)

เว็บไซต์นี้ได้รับการตกแต่งด้วยธีม **Dusty Rose Light Theme** มีโทนสีแสนอบอุ่น เรียบง่าย และสบายตา:


| ชื่อสีในระบบ (CSS Variable) | รหัสสี Hex | การใช้งานบนเว็บไซต์ |
| :--- | :--- | :--- |
| `--bg-primary` | `#fdf5f4` | สีพื้นหลังหลักของเว็บไซต์ (สีชมพูครีมระเรื่อ) |
| `--bg-navbar` / `--bg-card` | `#ffffff` | พื้นหลังของแถบเมนู และแผ่นการ์ดบทความ/หลักสูตร |
| `--bg-section-alt` / `--bg-footer` | `#f5e8e7` | สีพื้นหลังของสตรีมบทความ และแถบด้านล่างสุดของเว็บ |
| `--text-primary` | `#2d1a19` | สีตัวอักษรเนื้อหาหลัก (สีน้ำตาลอบอุ่นเข้ม) |
| `--text-secondary` | `#7a4f4d` | สีตัวอักษรข้อมูลย่อย (วันที่, เครดิต, รายละเอียดเล็ก) |
| `--accent` | `#AA7C7A` | **สีชมพูกลีบกุหลาบหม่น (Dusty Rose)** สีเด่นของโปรเจ็ค |
| `--accent-hover` | `#8f6563` | สีชมพูหม่นที่เข้มขึ้นเล็กน้อยเมื่อนำเมาส์ไปชี้ |

---

## ✍️ วิธีการเขียนหรือแก้ไขเนื้อหา (Editing Content)

เมื่อต้องการแก้ไขหรือเพิ่มบทความ/หลักสูตรใหม่ ให้คุณใช้โปรแกรม Editor ทั่วไป (เช่น VS Code) เปิดไฟล์ในไดเรกทอรี `content/` แล้วเขียนเนื้อหาในฟอร์แมตนี้ครับ:

### ตัวอย่างการแต่งบทความใน `content/blog/my-new-post.mdx`:
```markdown
---
title: "หัวข้อบทความใหม่ของคุณ"
description: "คำอธิบายสั้นๆ ของบทความเพื่อไปแสดงผลในการ์ดหน้าแรก"
date: "2026-05-31"
tags: ["เทคโนโลยี", "Coding"]
coverImage: ""
lang: "th"
---

# หัวข้อเรื่องระดับ 1 (h1)

นี่คือข้อความย่อหน้าธรรมดา คุณสามารถแต่งตัวอักษรเป็น **ตัวหนา** หรือ *ตัวเอียง* ได้ง่ายๆ

## หัวข้อย่อยระดับ 2 (h2)
* รายการสัญลักษณ์ข้อ 1
* รายการสัญลักษณ์ข้อ 2

### ตัวอย่างการเขียนโค้ดภาษา Python:
```python
def hello_world():
    print("สวัสดีครูทีกิด!")
```
```

---

## 🚀 คำสั่งควบคุมโปรเจ็ค (CLI Commands Reference)

เปิดหน้า Terminal และเข้าโฟลเดอร์โปรเจ็ค เพื่อใช้งานคำสั่งต่อไปนี้:

* **เปิดโปรเจ็คจำลองบนเครื่องเพื่อดูหน้าเว็บ**:
  ```bash
  npm run dev
  ```
  *(เปิดเบราว์เซอร์ดูได้ที่: http://localhost:3000)*

* **ทดสอบระบบการ Build ก่อนนำขึ้นโฮสต์จริง**:
  ```bash
  npm run build
  ```

* **เปิดรันโปรเจ็คในโหมด Production**:
  ```bash
  npm run start
  ```
